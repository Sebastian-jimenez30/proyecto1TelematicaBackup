version: '3.8'

services:
  mom1:
    build:
      context: ./mom
    container_name: mom1
    command: ["0.0.0.0", "/data", "50051"]
    ports:
      - "50051:50051"
    volumes:
      - ./mom/data/mom1:/data
    networks:
      - momnet

networks:
  momnet:
    driver: bridge
